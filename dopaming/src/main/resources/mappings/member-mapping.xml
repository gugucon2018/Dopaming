<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- dumy_mapper.xml -->
<mapper namespace="MemberDAO">
 	
 	<!-- 로그인 -->
    <select id="login" resultType="member">
    	select * from members where member_id=#{member_id} and member_password=#{member_password}
    </select>
    <!-- id 체크 -->
    <select id="valueCheckId" resultType="String">
     	select member_id from members where member_id = #{member_id}
    </select>
    <!-- 패스워드 체크 -->
    <select id="valueCheckPW" resultType="String">
     	select member_password from members where member_id = #{member_password}
    </select>
   <!-- 이메일 체크 -->
    <select id="valueCheckEmail" resultType="String">
    	select member_email from members where member_email = #{member_email}
    </select>
    <!-- 회원가입 -->
    <insert id="register">
    	insert into members (
    		member_id
    		, member_password
    		, member_email
    	) values (
    		#{member_id}
    		, #{member_password}
    		, #{member_email} 
    	)
    </insert>
    
</mapper>
